// hero.ts
import { heroui } from "@heroui/react";

export default heroui({
  themes: {
    dark: {
      colors: {
        background: "#101115",
        foreground: "#FFFFFF",
      },
    },
  },
});
